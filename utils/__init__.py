# =============================================================================
# UTILS PACKAGE INITIALIZATION
# =============================================================================
# This is the __init__.py file for the 'utils' package.
# 
# In Python, a folder becomes an "importable package" when it contains an
# __init__.py file. This allows other parts of the project to do:
#   from utils.data_loader import load_all_data
#   from utils.filters import create_sidebar_filters
# 
# This file can be empty (as it largely is here), or it can contain:
# - Package-level imports
# - Package documentation
# - Package initialization code
# 
# For this dashboard, the __init__.py is minimal because we import modules
# directly (e.g., from utils.data_loader import ...) rather than importing
# from the package level (e.g., from utils import ...).
# =============================================================================

# Utils package for Paris 2024 Olympics Dashboard
